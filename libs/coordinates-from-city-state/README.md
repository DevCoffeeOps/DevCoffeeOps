# coordinates-from-city-state

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test coordinates-from-city-state` to execute the unit tests via [Jest](https://jestjs.io).
