import { googleMapsSearchText } from './google-maps-search-text';

describe('googleMapsSearchText', () => {
  it('should work', () => {
    expect(googleMapsSearchText()).toEqual('google-maps-search-text');
  });
});
